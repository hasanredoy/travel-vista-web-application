import Login from "@/pages/login/Login";
export const metadata = {
  title: "Login",
  description: "Access your Travel Vista account to manage bookings, reviews, and preferences. Log in now and continue your journey.",
};

const LoginPage = () => {
  return (
  <Login></Login>
  );
};

export default LoginPage;